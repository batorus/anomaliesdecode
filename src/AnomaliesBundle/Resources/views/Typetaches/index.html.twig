{% extends 'AnomaliesBundle::base.html.twig' %}

{% block typetaches_list -%}
    <h1>Typetaches list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Typetache</th>
                <th>Enabled</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('typetaches_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.typetache }}</td>
                <td>{{ entity.enabled }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('typetaches_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('typetaches_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('typetaches_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
